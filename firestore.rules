rules_version = '2';

// All application access goes through Cloud Functions (Admin SDK bypasses
// these rules). Client SDK access is denied entirely.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
