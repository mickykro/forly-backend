rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Admin-only. Web access via Functions. Custom JWT ≠ Firebase Auth, so client SDK blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}