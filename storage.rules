rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Admin-only. Public media via tokened URLs from downloadAndUpload (don't require client auth).
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
